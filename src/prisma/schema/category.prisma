
model Category {
    id              Int         @id @default(autoincrement())
    nama            String
    deskripsi       String  

    book            Book[]

    createdAt       DateTime    @default(now())
    updatedAt       DateTime    @updatedAt
    
    deletedAt       DateTime? 
     @@map("categories")
}